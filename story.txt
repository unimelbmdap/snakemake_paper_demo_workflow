I need to access the coregistered anatomical `results/sub-10159/anat/sub-10159_T1w_coreg.nii.gz`

	# coreg
	I know how to produce `results/sub-{subj_id}/anat/sub-{subj_id}_T1w_coreg.nii.gz`
	But I need to have the raw anatomical image `data/sub-10159/anat/sub-10159_T1w.nii.gz`.
	I also need to have the mean functional image `results/sub-10159/func/sub-10159_mean.nii.gz`

		# storage
		I have `data/sub-10159/anat/sub-10159_T1w.nii.gz` in the raw data storage

		# tmean
		I know how to produce files of the form `results/sub-{subj_id}/func/sub-{subj_id}_mean.nii.gz`, so I can do that.
		But I need to have the images `results/sub-{subj_id}/func/sub-{subj_id}_task-stopsignal_bold.nii.gz` and `results/sub-{subj_id}/func/sub-{subj_id}_task-taskswitch_bold.nii.gz`

			# mc
			I know how to produce `results/sub-{subj_id}/func/sub-{subj_id}_task-{task}_bold.nii.gz`, so I can do that.
			But I need to have the images `data/sub-{subj_id}/func/sub-{subj_id}_task-{task}_bold.nii.gz`

				# storage
				I have `data/sub-{subj_id}/func/sub-{subj_id}_task-{task}_bold.nii.gz` in the raw data storage
